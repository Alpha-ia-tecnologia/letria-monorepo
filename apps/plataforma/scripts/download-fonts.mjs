import fs from 'node:fs/promises';
const url='https://fonts.googleapis.com/css2?family=Nunito:wght@400;500;600;700;800;900&family=DM+Sans:wght@400;500;600;700&display=swap';
const response=await fetch(url,{headers:{'User-Agent':'Mozilla/5.0'}});if(!response.ok)throw new Error('font css');let css=await response.text();await fs.mkdir('public/fonts',{recursive:true});const urls=[...new Set([...css.matchAll(/url\((https:[^)]+)\)/g)].map(m=>m[1]))];for(const [i,u]of urls.entries()){const r=await fetch(u);if(!r.ok)throw new Error('font download');const ext=u.includes('.woff2')?'woff2':u.includes('.woff')?'woff':'ttf';const path=`/fonts/letria-${i}.${ext}`;await fs.writeFile(`public${path}`,new Uint8Array(await r.arrayBuffer()));css=css.replaceAll(u,path);}await fs.writeFile('public/fonts/fonts.css',css);const global=await fs.readFile('app/globals.css','utf8');await fs.writeFile('app/globals.css',global.replace(/^@import url\([^\n]+\);/m,"@import url('/fonts/fonts.css');"));console.log(`${urls.length} fontes salvas localmente.`);
